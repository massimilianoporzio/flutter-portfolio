import 'package:flutter/material.dart';

class HeroSmallButtons extends StatelessWidget {
  const HeroSmallButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
