import 'package:flutter/gestures.dart';
import 'package:flutter/material.dart';

class HeroLargeButtons extends StatelessWidget {
  const HeroLargeButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
